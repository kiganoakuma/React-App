import { useState } from "react";
import Form from "./components/Form";

const App = () => {
  const handleClick = () => {};

  return (
    <div>
      <Form />
    </div>
  );
};

export default App;
